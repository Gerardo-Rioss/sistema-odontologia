/**
 * Global Jest setup file.
 *
 * Extends Jest with custom DOM matchers from @testing-library/jest-dom,
 * providing assertions like `toBeInTheDocument()`, `toHaveAttribute()`, etc.
 */
import "@testing-library/jest-dom";
